# Copy to .env (or let `uv run quickstart` write one for you).
MLFLOW_EXPERIMENT_ID=
MLFLOW_TRACKING_URI=databricks
APX_SMOKE_MODE=1
# Set for production — points to the Genie space with account data
# ACCOUNT_GENIE_SPACE_ID=
