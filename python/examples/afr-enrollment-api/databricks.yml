bundle:
  name: afr-enrollment-api

targets:
  dev:
    mode: development
    workspace:
      host: ${workspace.host}

resources:
  apps:
    afr-enrollment-api:
      name: afr-enrollment-api
      source_code_path: .
