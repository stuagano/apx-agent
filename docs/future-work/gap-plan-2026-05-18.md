# apx-agent — Gap Plan (2026-05-18, last updated 2026-05-19)

A snapshot of what's shipped, what's missing, and the order I'd close the gaps. Living doc — update as items land or the priority order shifts.

**Update 2026-05-19**: Watchdog integration's three blocking contracts answered (UC tags / MCP tools / UC violations table). Implementation shipped — see [Resolved contracts](#resolved-contracts-2026-05-19) below.

## Where we are

Everything on the original Google ADK comparison is shipped. 535 tests passing across 12 commits in this iteration.

| ADK surface | Status | apx-agent module |
|---|---|---|
| Agent primitives | ✅ | `_agents.py` |
| Workflow agents (Sequential/Parallel/Loop/Router/Handoff) | ✅ | `_agents.py` |
| Agent-as-tool (LLM-driven composition, local + remote) | ✅ | `_agent_tool.py` |
| `@tool` decorator with UC sync | ✅ | `_tool.py`, `_tool_publish.py` |
| Platform tool factories | ✅ | `genie.py`, `catalog.py`, `vector_search.py`, `sql_tools.py`, `foundation_model.py` |
| Sessions + state | ✅ | `_session.py`, `_session_delta.py`, `_session_lakebase.py` |
| MemoryBank (durable + semantic recall) | ✅ shipped 2026-05-19 | Python: `_memory.py`, `_memory_lakebase.py` (pgvector), `_memory_delta.py` (with `VectorSearchLike` delegation), `_memory_tools.py`, `_memory_consolidate.py`; TS: `memory.ts`, `memory-lakebase.ts`, `memory-delta.ts`, `memory-tools.ts`, `memory-consolidate.ts` |
| ExampleStore (few-shot retrieval) | ✅ shipped 2026-05-19 | Python: `_example.py`, `_example_lakebase.py`, `_example_delta.py`, `_example_tools.py`, `_example_mining.py` (cold-start from session history); TS: `example.ts`, `example-lakebase.ts`, `example-delta.ts`, `example-tools.ts`, `example-mining.ts` |
| Prompt assembly helpers | ✅ shipped 2026-05-19 | Python: `_prompt_assembly.py` (`assemble_memory_context` / `assemble_example_context` / `assemble_context`); TS: `prompt-assembly.ts` |
| CLI: `apx memory` + `apx examples` | ✅ shipped 2026-05-19 | `recall`/`remember`/`forget`/`list` + `find`/`save`/`remove`/`list` with `--store-module MODULE:VAR` injection and `pyproject [tool.apx.agent].memory_store`/`example_store` fallback |
| Worked example: memory_demo | ✅ shipped 2026-05-19 | `python/examples/memory_demo/` — standalone runnable demo using `make_memory_tools` + `assemble_context` |
| Callbacks (before/after tool & model) | ✅ | `_callbacks.py` |
| Eval framework | ✅ | `_eval.py` |
| MLflow experiments | ✅ | `_chat_agent.py`, `_eval.py`, `cli.py` |
| Deployment (Model Serving + Apps) | ✅ | `_chat_agent.py`, `_wiring.py` |
| Observability (auto MLflow tracing) | ✅ | `_mlflow_tracing.py` |
| Managed MCP URLs + client config | ✅ | `_managed_mcp.py` |
| Publish to Supervisor | ✅ | `_publish.py` |
| CLI (`scaffold/run/eval/deploy/publish-tools/publish/mcp-config/info/logs/version`) | ✅ | `cli.py` |
| Worked example | ✅ | `examples/customer_triage/` |

## Gaps — what's still missing

### Governance & safety

The governance story splits cleanly: **runtime enforcement** (the hooks apx-agent already exposes) and **compliance posture** (cross-domain policy evaluation, violation tracking, owner accountability). The runtime layer is apx-agent's; the posture layer is [databricks-watchdog](https://github.com/stuagano/databricks-watchdog)'s. Integration > reimplementation.

| Gap | Why it matters | Effort |
|---|---|---|
| ~~**Watchdog integration**~~ ✅ shipped 2026-05-19 — `_watchdog.py` (864 lines, 38 tests). Three wire-protocol contracts wired: UC tags via `set_uc_tags_for_agent` (crawler input), MCP via `make_mcp_transport` (runtime policy decisions through `mcp.client.streamable_http`), UC Delta table via `make_uc_violation_writer` (runtime violation reports). `WatchdogClient` + `WatchdogGuard` + `compose` wire decisions into the existing hooks; reject/redact decisions auto-emit `apx.watchdog.*` audit attrs onto the active span so downstream consumers (and watchdog's own crawler) see runtime activity. Read-side: `apx watchdog status` + `apx watchdog violations` for inspecting compliance posture from the CLI alongside `apx cost` / `apx trace`. | M |
| ~~**Audit log helpers**~~ ✅ shipped 2026-05-19 | ~~A standard span attribute set so every framework-emitted MLflow trace records the same audit fields (caller identity, tool inputs, tool outputs, sub-agent endpoint hit).~~ Now lives in `_audit.py` as `AuditAttrs` + `set_audit_attrs`. Wired through `_chat_agent.py`, `_callbacks.py`, and `_watchdog.py`. Watchdog can query the trace table by `apx.*` keys. | S |
| ~~**Local lightweight guards**~~ ✅ shipped 2026-05-19 — `_guards.py`: `RateLimit`, `ToolAllowlist`, `ToolDenylist`, `prompt_injection_heuristic`, `compose`. Pair with `WatchdogGuard` for layered governance. | S — ~100 lines |

### Observability

| Gap | Why it matters | Effort |
|---|---|---|
| **`apx trace`** | Inspect recent MLflow traces from the CLI. Pairs with `apx logs` (raw logs) and `apx info` (static introspection) for a complete operate surface. | S |
| ~~**Cost-tracking helpers**~~ ✅ shipped 2026-05-19 — `_cost.py`: `cost_for_agent`, `cost_for_endpoint`, `CostBreakdown`. Queries `system.billing.usage` joined to `system.billing.list_prices`. CLI `apx cost --agent X --hours N`. Partial pricing surfaces `total_usd=None` rather than understating. | M |
| ~~**Prompt + response log exporter**~~ ✅ shipped 2026-05-19 — `_trace_export.py` with `export_traces`. MERGE upsert into a Delta table with flat schema (trace_id, agent_name, operation, status, latency, session_id, watchdog_action, …). CLI: `apx export-traces --table ... --hours N`. | M |

### Multi-agent & topology

| Gap | Why it matters | Effort |
|---|---|---|
| **`apx list`** | Discover all deployed apx-agents in a workspace via UC + serving endpoints. Useful for fleet operators. | S |
| ~~**Topology visualization**~~ ✅ shipped 2026-05-19 — `_topology.py`: `discover_topology(ws)` + `render_topology(topo, format=mermaid|graphviz)`. CLI: `apx topology --format mermaid`. | M |
| ~~**Cross-agent evaluation**~~ ✅ shipped 2026-05-19 — `_eval_chain.py`: `evaluate_chain(agent, model, evalset, experiment)` runs eval then correlates per-prompt sub-agent invocations via MLflow trace span attrs (`apx.subagent.endpoint`, `apx.tool.name`). CLI: `apx eval-chain`. | M |

### Developer experience

| Gap | Why it matters | Effort |
|---|---|---|
| **`apx test`** | Local smoke test — send a couple of sample queries to a compiled agent, verify they don't crash. Cheaper than `apx eval` for "did I break the import?" checks. | S |
| ~~**TypeScript parity**~~ ✅ four waves shipped 2026-05-19 | **22+ modules** ported in parallel via fanned-out agents. **Wave 1**: `audit.ts` (30), `guards.ts` (34), `cost.ts` (21), `canary.ts` (19), `managed-mcp.ts` (14), `topology.ts` (10). **Wave 2**: `resources.ts` (30), `trace-export.ts` (21), `run-once.ts` (16), `hot-swap.ts` (17), `lint.ts` (26). **Wave 3**: `tool.ts` + `tool-publish.ts` (28 — `defineUcTool` factory variant + injectable `UcFunctionClient`), `watchdog.ts` (47 — full integration), `publish.ts` (18 — Supervisor sub-agent registration), `session-delta.ts` (18) + `session-lakebase.ts` (15), `evaluate.ts` (34 — injectable `mlflowEvaluate`). **Wave 4**: `evaluate-chain.ts` (24 — trace correlation via injectable `traceSearch`), `chat-agent.ts` (26 — `compileToChatAgent` + `logAgent` with injectable `mlflowLogModel`/`materializeResources`), `cli/*` (23 — commander-based `apx` starter with `info`/`list`/`lint`/`test`/`version`, regex pyproject parser, `bin: apx`), `mlflow-tracing.ts` (22) + `invocations.ts` (18 — Express bridge with NDJSON streaming). **511 new TS tests across all four waves**, all passing (3 unrelated pre-existing failures). Project typecheck clean. **predict stream limitation**: TS `runViaSDK` only surfaces the final assistant message, so `predictStream` yields one chunk — documented; API shape preserved for future swap. | XS — surface complete |
| ~~**Agent linting**~~ ✅ shipped 2026-05-19 | `_lint.py` with `lint_agent(agent, *, model=None) -> list[LintFinding]` plus CLI `apx lint`. Tier-1 rules: L001 missing instructions (error), L002 tool with no docstring (warning), L003 sub-agent URL referencing unset `$ENV_VAR` (error), L004 model name doesn't match known Databricks prefixes (warning). Walks composite agents (Sequential/Parallel/Loop/Router/Handoff). Exits non-zero on any error; pair with `apx test` (smoke) and `apx eval` (behavior) for pre-deploy. | M |
| ~~**Hot-swap model**~~ ✅ shipped 2026-05-19 | `_hot_swap.py`: `hot_swap_model(endpoint, model, *, ws=None, env_var=APX_MODEL_OVERRIDE_ENV, wait=True)` rewrites each served entity's `environment_vars` dict to set `APX_AGENT_MODEL_OVERRIDE=<new model>`. Returns `HotSwapResult` with the previous override value (for one-call rollback). `get_active_override(endpoint)` reads the current value. Runtime side: `_chat_agent.py::_resolve_model()` consults the env var before falling back to `self._model` so both `predict()` and `predict_stream()` honor the swap on next replica start. CLI: `apx hot-swap --endpoint X --model Y`. Different concern from canary: this rewrites env vars on the existing served entity (no new artifact); canary creates new served entities with traffic split (new version per swap). | M |

### Production patterns

| Gap | Why it matters | Effort |
|---|---|---|
| ~~**Canary / A-B helpers**~~ ✅ shipped 2026-05-19 — `_canary.py`: `deploy_canary(endpoint, model, version, traffic_pct)` proportionally redistributes existing traffic and adds the canary, `promote_canary` / `rollback_canary` flip to 100% to a chosen version (other entities stay configured at 0% for fast rollback), `analyze_canary(endpoint, experiment, hours)` partitions MLflow traces by served-entity version and reports requests / errors / latency P50 P95 / avg per version with `best_by_latency` / `best_by_error_rate` tie-breaking. CLI: `apx canary {status,deploy,promote,rollback,analyze}`. | M |
| ~~**Feature flags for tools**~~ ✅ shipped 2026-05-19 | `FeatureFlagGuard` in `_guards.py` — gates listed tools by an external flag provider. Plugs into `before_tool` via `compose()`. Per-user/per-tenant gating works by capturing context in the provider's closure. | S |
| ~~**Scheduled-job mode**~~ ✅ shipped 2026-05-19 | `run_once(agent, prompt, *, model=None)` in `_run_once.py`. Compiles via `compile_to_chat_agent` and returns the final assistant text. Model defaults to `[tool.apx.agent].model` in pyproject. Auth picks up `DATABRICKS_CLIENT_ID`/`SECRET` when set (Databricks Jobs inject these). Collapses ~150-line hand-rolled agent-loop reimplementations down to a single call. | S |

### Dogfooding

| Gap | Why it matters | Effort |
|---|---|---|
| ~~**Port shortage-intelligence to the new primitives**~~ ⚠️ partial 2026-05-19 — Dry-run analysis in `MIGRATION.md` confirms 5 of 5 data-fetching tools must stay Python-only (user-scoped OBO). Shipped: README pointing at `apx deploy` (which now chains publish-tools + log_agent + agents.deploy + set_uc_tags in one command), `evalset.jsonl` for `apx eval-chain`, and `classify_shortage_severity` extracted as `@tool(uc=...)` to demonstrate the UC-function pattern. Session/watchdog wiring documented but not implemented (depend on UX / watchdog availability). | M — needs domain knowledge |

## Recommended order

1. ~~**Watchdog integration**~~ ✅ — shipped end-to-end with UC tags / MCP / UC violations.
2. ~~**Audit log helpers**~~ ✅ — shipped as `AuditAttrs` + `set_audit_attrs`, wired through chat_agent / callbacks / watchdog.
3. ~~**`apx trace`**~~ ✅ — `mlflow.search_traces` with `apx.*` attribute filters and text/json output.
4. ~~**`apx test`**~~ ✅ — local smoke test that compiles + sends sample prompts; non-zero exit on failure.
5. ~~**`apx list`**~~ ✅ — UC tag scan for `apx.agent.name` across registered models.
6. ~~**Local lightweight guards**~~ ✅ — `_guards.py` with `RateLimit` / `ToolAllowlist` / `ToolDenylist` / `prompt_injection_heuristic` / `compose`.
7. ~~**Cost-tracking helpers**~~ ✅ — `_cost.py` + `apx cost`. Queries `system.billing.usage` × `system.billing.list_prices`.
8. **TypeScript parity** — biggest gap but biggest effort. Tackle once the Python surface is stable. Plan: copy the Python module list into a TS skeleton, prioritize `Agent`, `@tool`, log/deploy, then expand.
9. **Canary helpers** — production move, lower urgency until customers are at scale.
10. ⚠️ **Dogfood shortage-intelligence** — partial 2026-05-19: dry-run analysis + deploy/eval scaffolding + one UC tool shipped; session/watchdog wiring documented but not implemented. See `examples/shortage-intelligence-agent/MIGRATION.md`.

## Picked: Watchdog integration

Swapping the pick from "policies library" to "Watchdog integration" because [databricks-watchdog](https://github.com/stuagano/databricks-watchdog) is the compliance posture layer for UC that already does cross-domain policy evaluation, ontology with inheritance, violation lifecycle, owner accountability, and AI agent governance as a first-class domain. Building a local policies library inside apx-agent would duplicate (worse) what watchdog already ships.

### The split

| Layer | Owner | Lives where |
|---|---|---|
| **Runtime hooks** — `input_guardrails`, `output_guardrails`, `before_tool`, `before_model`, etc. | apx-agent | `_compile.py` + `_callbacks.py` (already shipped) |
| **Compliance posture** — declarative policies, violation tracking, owner accountability, trends | watchdog | `databricks-watchdog` repo |
| **Bridge** — emit / consume / report between the two | apx-agent (this work) | new `_watchdog.py` module |

### Integration shape

Three concrete pieces:

1. **Emit agent metadata for watchdog crawls.** apx-agent already knows the resources an agent declares (UC functions, Genie spaces, serving endpoints, vector indexes — via `collect_resource_specs`). A helper that writes those plus instructions / tool descriptions / sub-agent endpoints to a stable location (an MLflow tag, a UC table, or a JSON document at a known endpoint path) lets watchdog's crawler pick them up alongside other UC assets.

2. **Consume watchdog policy decisions at runtime.** A `WatchdogGuard` adapter that calls watchdog's policy-evaluation surface (one of its 13 MCP tools, or a direct API) for a given operation context, and returns reject/allow/redact. Plugs into the existing `input_guardrails` / `before_tool` / `before_model` hooks. Cached per-resource so it doesn't add a network hop on every tool call.

3. **Report decisions back as violation signals.** When the runtime blocks something via a `WatchdogGuard`, log a violation back to watchdog so the compliance dashboard reflects what actually happened in production — not just static crawl results. Pairs naturally with the audit log helpers (item #2 in the recommended order).

### Resolved contracts (2026-05-19)

All three blocking questions answered. Implementation shipped.

| Contract | Mechanism | Module |
|---|---|---|
| Metadata for watchdog's crawler | UC tags on the registered model (`apx.agent.*`) | `set_uc_tags_for_agent` |
| Runtime policy decisions | Watchdog's MCP tools (streamable HTTP) | `make_mcp_transport(mcp_url, tool_name=...)` |
| Violation reports | INSERT into a watchdog-owned UC Delta table | `make_uc_violation_writer(violations_table, ws=...)` |

`make_watchdog_transport` bundles the MCP evaluate transport + UC violation writer into one callable that `WatchdogClient(transport=...)` consumes. The UC violation writer auto-creates the violations table on first use; the MCP transport invokes the configured tool via `mcp.client.streamable_http`. Tests use mocked transports/clients (570 passing).

Status: integration is real, not sketch. Verification against a live watchdog deployment is the remaining work — see `_watchdog.py`'s docstring for the expected MCP tool response shape (`{"action", "reason", "policy_id", "domain", "redacted_content", "metadata"}`) which the watchdog side needs to match.
