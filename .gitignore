# Python
__pycache__/
*.pyc
.venv/
*.egg-info/

# TypeScript
node_modules/
*.tsbuildinfo

# Build
dist/

# Databricks Apps deploy configs (contain client secrets)
typescript/deploy/*/app.yaml

# Claude Code skill scratch
.superpowers/

# Claude / agent IDE state
.claude/
.antigravitycli/

# Git worktrees
.worktrees/

# Local MLflow runs
mlruns/

# Auto-generated backup files (dotfile-safety rule, etc.)
*.bak.*
.gitignore.bak.*

# Databricks Apps local state
**/builder.db
**/.databricks-resources.json
**/.env.local
**/.env.local.bak*

# Generated package version files
**/_version.py

# Local reference clones (not part of the project)
/reference/

# Local scaffold sandboxes (developer's own apx-agent scratch projects)
python/apxshared/
python/my-agent/
python/my_coworker/
my-agent.yaml
python/my-payroll.yaml

# Local MLflow tracking DBs (created by dev runs / smoke tests)
**/mlflow.db

# Playwright MCP scratch (console logs / page snapshots)
.playwright-mcp/

# Dev-UI scratch screenshots
devui-*.png

# Apps deploy artifacts — built from source by `databricks bundle deploy`
**/.build/
**/apx_agent-*.whl
**/.env
**/dist/
**/__dist__/
package-lock.json
# ...except the TS package's lockfile, which CI's `npm ci` needs.
!typescript/package-lock.json

# direnv per-shell env (personal — pins gh account for git push)
.envrc
.envrc.local
.apx.local

# Personal/internal use-case writeup (not for the public repo)
docs/Built on APX-Agent UseCase.md
docs/omniagent/

# Local scratch — PR diffs, gap analysis, registry snapshots (not for the repo)
.polly/

# Client-specific demos — built locally, not shipped or tracked
demos/

# Superpowers planning artifacts (specs/plans) — local, not tracked
docs/superpowers/

# Client-specific demos — built locally, not shipped or tracked
demos/

# Superpowers planning artifacts (specs/plans) — local, not tracked
docs/superpowers/
