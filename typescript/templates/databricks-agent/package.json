{
  "name": "{{AGENT_NAME}}",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npx tsc --project tsconfig.json",
    "build:bundle": "node esbuild.config.mjs",
    "start": "node dist/app.cjs",
    "dev": "npx tsx app.ts",
    "deploy": "./deploy.sh",
    "deploy:build-only": "./deploy.sh build-only"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "express": "^5.1.0",
    "zod": "^4.0.0",
    "zod-to-json-schema": "^3.25.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "esbuild": "^0.28.0",
    "typescript": "^6.0.0"
  }
}
