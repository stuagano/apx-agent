# esbuild bundle — no npm install on Databricks Apps
command:
  - "node"
  - "dist/app.cjs"

env:
  - name: PORT
    value: "8000"
  - name: NODE_ENV
    value: "production"
  - name: DATABRICKS_TOKEN
    value: "{{DATABRICKS_TOKEN}}"
